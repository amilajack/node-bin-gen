const node = require.resolve('../@amilajack/node');
const { execSync } = require('child_process');

describe('basic tests', () => {
    it('should get the node version', () => {
        execSync('')
    });
});